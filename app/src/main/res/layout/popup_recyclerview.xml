<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/colorWhite"
    tools:ignore="MissingConstraints">

    <android.support.v7.widget.RecyclerView
        android:id="@+id/rec"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <TextView
        android:paddingTop="8dp"
        android:paddingBottom="8dp"
        android:paddingStart="40dp"
        android:paddingEnd="40dp"
        android:background="@drawable/corner_orange_esay"
        android:id="@+id/bt_buy"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="智慧豆"
        android:textColor="@color/colorWhite"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toLeftOf="@id/bt_ok"
        app:layout_constraintTop_toBottomOf="@id/rec" />

    <TextView
        android:paddingTop="8dp"
        android:paddingBottom="8dp"
        android:paddingStart="40dp"
        android:paddingEnd="40dp"
        app:layout_constraintTop_toTopOf="@id/bt_buy"
        android:background="@drawable/corner_orange_deep"
        android:id="@+id/bt_ok"
        android:textColor="@color/colorWhite"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="赠送"
        app:layout_constraintLeft_toRightOf="@id/bt_buy"
        app:layout_constraintRight_toRightOf="parent" />
</android.support.constraint.ConstraintLayout>
